apiVersion: v1
kind: Service
metadata:
  name: {{ include "metabase-app.fullname" . }}
  labels:
    {{- include "metabase-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metabase-app.selectorLabels" . | nindent 4 }}
